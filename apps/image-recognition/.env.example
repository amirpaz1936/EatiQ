PORT=3001
OPENAI_API_KEY=
OPENAI_VISION_MODEL=gpt-4o-mini

# Set to "base64" for local Minio dev so OpenAI doesn't need to reach our bucket
# (the docker-compose stack already wires this for the image-recognition service).
# Leave unset (or set to "url") in production.
IMAGE_FETCH_MODE=
